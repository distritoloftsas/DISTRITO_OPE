# Copiar a .env y ajustar valores reales. NUNCA subir .env al repositorio.

# --- PostgreSQL local (Docker Compose) ---
DB_HOST=localhost
DB_PORT=5433
DB_NAME=distrito_loft
DB_USER=distrito
DB_PASSWORD=distrito_local_dev

# --- JWT (cambiar en producción) ---
JWT_SECRET=cambiar-este-secreto-en-produccion-debe-ser-largo-y-aleatorio
JWT_EXPIRATION_MS=86400000

# --- Backend ---
SERVER_PORT=8080
